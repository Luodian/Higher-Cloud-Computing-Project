#!/bin/bash
#created by elrond wang 
#2017/12/3 21:39
#version 1.0
#welcome

echo $1 | sudo -S chmod +rwx $HOME
echo $1 | sudo -S chmod 700 $HOME/.ssh
if [ -e $HOME/.ssh/authorized_keys ]
then
    echo $1 | sudo -S chmod +rw $HOME/.ssh/authorized_keys
fi
echo "the temporarily competence has been changed in order to adapt to the ssh requirement"
exit 0
